[package]
name = "petri-executor-host"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The host executor: a workspace directory and real processes on this machine."
publish = false

[lints]
workspace = true

[lib]
# The package is `petri-executor-host`; the crate is `executor_host`, so code reads `executor_host::…`.
name = "executor_host"

[dependencies]
executor = { workspace = true }
ir = { workspace = true }
async-trait = { workspace = true }
libc = { workspace = true }
smol_str = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
