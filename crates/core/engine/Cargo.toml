[package]
name = "petri-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure, sans-IO token-flow state machine over the ir crate."
publish = false

[lib]
# The package is `petri-engine`; the crate is `engine`, so code reads `engine::…`.
name = "engine"

[dependencies]
ir = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
smol_str = { workspace = true }
thiserror = { workspace = true }
