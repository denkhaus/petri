[package]
name = "petri-executor"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The interface between the driver and the environments that run steps."
publish = false

[lib]
# The package is `petri-executor`; the crate is `executor`, so code reads `executor::…`.
name = "executor"

[dependencies]
ir = { workspace = true }
async-trait = { workspace = true }
libc = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
smol_str = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
