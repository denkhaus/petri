[package]
name = "petri-frontend-native"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The native workflow format to HIR."

[lib]
# The package is `petri-frontend-native`; the crate is `frontend_native`, so code reads `frontend_native::…`.
name = "frontend_native"

[dependencies]
ir = { workspace = true }
frontend = { workspace = true }
serde_json = { workspace = true }
smol_str = { workspace = true }

[dev-dependencies]
steps = { workspace = true }
engine = { workspace = true }
driver = { workspace = true }
executor = { workspace = true }
executor-host = { workspace = true }
tokio = { workspace = true }
