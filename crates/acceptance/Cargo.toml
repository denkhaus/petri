[package]
name = "petri-corpus"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The compatibility corpus harness: real workflows through the GHA frontend."
publish = false

[lib]
# The package is `petri-corpus`; the crate is `corpus`, so code reads `corpus::…`.
name = "corpus"

[dependencies]
ir = { workspace = true }
frontend = { workspace = true }
frontend-gha = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
engine = { workspace = true }
driver = { workspace = true }
executor = { workspace = true }
executor-host = { workspace = true }
steps = { workspace = true }
tokio = { workspace = true }
