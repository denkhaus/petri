[package]
name = "petri-testkit"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared test scaffolding: run directories, report readers, the wedged step, the gh stub."
publish = false

[lib]
# The package is `petri-testkit`; the crate is `testkit`, so code reads `testkit::…`.
name = "testkit"

[dependencies]
ir = { workspace = true }
engine = { workspace = true }
executor = { workspace = true }
executor-docker = { workspace = true }
steps = { workspace = true }
driver = { workspace = true }
async-trait = { workspace = true }
serde_json = { workspace = true }
smol_str = { workspace = true }
tokio = { workspace = true }
