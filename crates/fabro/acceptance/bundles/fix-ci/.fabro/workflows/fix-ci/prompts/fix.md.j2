Fix the deterministic CI failures from the preceding check step.

The workflow checked out the existing target branch in
`.fabro/workflows/fix-ci/runtime/target`. Read its `AGENTS.md` before you make
changes. Run all repository commands from that directory.

Read the failed check output in the current context. Full logs and a summary
are in `.fabro/workflows/fix-ci/runtime/logs/` and
`.fabro/workflows/fix-ci/runtime/check-summary.txt`.

Inspect the affected code and make the smallest correct change that fixes all
reported failures. Preserve the intended behavior of the branch. Do not weaken
tests, lint rules, type checks, or security checks to make them pass.

Keep the repair small and limited to the reported CI failures.

Do not commit, amend, push, change branches, open a pull request, install
packages, or use the network. The workflow owns validation and publication.
Do not edit the Factory repository outside the target runtime checkout.

Before you finish, inspect the target repository's `git diff` and summarize the
changes you made. The workflow will run the complete deterministic check set
again.
