id = "malformed-skills-reported"
title = "Malformed skill files are reported"
scenarios = ["*"]
bundles = ["*"]
fabro = "Skipped silently."
petri = "A `fabro.skills.warning` event and a stderr line per skipped `SKILL.md`, from Pebble's `SkillsDiscovered.skipped` report; the file is still skipped."
user_visible_effect = "Diagnostics only; the loaded skills are the same."
reason = "Never a silent skip."
acceptance = "Prompt content compared through the twins is identical."
