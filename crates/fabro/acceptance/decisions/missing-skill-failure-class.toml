id = "missing-skill-failure-class"
title = "A missing /name skill fails with class skill_missing"
scenarios = ["*"]
bundles = ["*"]
fabro = "The stage fails with an `InvalidState` error."
petri = "The node fails with class `skill_missing`."
user_visible_effect = "A different failure class on the same failed stage."
reason = "Petri's failure classes are its own vocabulary."
acceptance = "The stage outcome (`failed`) and routing are compared; the class is bookkeeping."
