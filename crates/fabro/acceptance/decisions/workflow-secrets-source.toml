id = "workflow-secrets-source"
title = "Workflow secrets resolve from PETRI_SECRET_NAME"
scenarios = ["*"]
bundles = ["*"]
fabro = "The platform vault."
petri = "`{{ secrets.NAME }}` from `PETRI_SECRET_NAME` in the standalone runner."
user_visible_effect = "Configuration only; the resolved values are the same."
reason = "Task 7."
acceptance = "No projection difference; the differential harness gives both engines the same values."
