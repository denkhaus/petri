id = "fallback-resolution"
title = "Fallback chain resolution differs for provider-only and unknown selectors"
scenarios = ["fallback-*"]
bundles = ["*"]
fabro = "A bare provider picks the provider's closest model; an unknown selector passes through."
petri = "A bare provider is the same model id on that provider; an unknown selector is skipped with a notice. A bad chain fails run start on both engines since 2026-09-13; since 2026-09-17 Petri refuses it at load (`attractor.model.fallbacks`, `attractor.model.unknown`) when the runner has a model client, as Fabro refuses it at run creation."
user_visible_effect = "Different target models for provider-only chains."
reason = "Task 12; `lithos-llm` has no feature-profile matcher."
acceptance = "Scoped to fallback scenarios that use such chains; none does today."
