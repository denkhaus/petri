id = "fallback-resolution"
title = "Fallback chain resolution differs for provider-only and unknown selectors"
scenarios = ["fallback-*"]
bundles = ["*"]
fabro = "A bare provider picks the provider's closest model; an unknown selector passes through; a bad chain fails run start."
petri = "A bare provider is the same model id on that provider; an unknown selector is skipped with a notice; a bad chain fails the first LLM stage with class `bad_config`."
user_visible_effect = "Different target models for provider-only chains; a configuration error surfaces later."
reason = "Task 12; `lithos-llm` has no feature-profile matcher."
acceptance = "Scoped to fallback scenarios that use such chains; none does today."
