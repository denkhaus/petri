id = "acp-tool-hooks-best-effort"
title = "ACP tool hooks are best effort with a warning"
scenarios = ["*"]
bundles = ["*"]
fabro = "Ignored silently."
petri = "Best effort at the two boundaries ACP offers: `pre_tool_use` at `session/request_permission` (a block rejects the call), `post_tool_use` and `post_tool_use_failure` at a reported `tool_call_update`; an `attractor.hook.warning` per configured hook says what it can see, and one per tool call the agent ran without asking."
user_visible_effect = "A warning per configured tool hook, and per unasked tool call, on Petri."
reason = "Never silently skip a configured tool hook (readiness item 1)."
acceptance = "Events only; no projection difference."
