id = "event-kinds"
title = "Petri's public events are its own kinds"
scenarios = ["*"]
bundles = ["*"]
fabro = "`stage.prompt`, `prompt.completed`, `agent.mcp.*`, `agent.failover`, ..."
petri = "`StepEvent::Custom` kinds for what Pebble cannot know (`fabro.prompt`, `fabro.prompt.completed`, `fabro.thread`, `fabro.fallback.plan`, `fabro.mcp.unavailable`, `fabro.compaction`, `fabro.skills`, `fabro.hook`, ...), and Pebble's own `CodingAgentEvent`s as `agent_activity` for every agent fact (decision `pebble-events-are-the-agent-contract`)."
user_visible_effect = "Event consumers differ; run results do not."
reason = "Readiness item 1: identical platform events are not required."
acceptance = "Events are not compared; projections are."
