[package]
name = "petri"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "A token-flow workflow engine: core, plus every component it ships with."

[dependencies]
runtime = { workspace = true }
frontend-gha = { workspace = true }
