# sandbox-driver is a private git dependency. Cargo's built-in fetcher cannot
# authenticate to it; the git CLI uses the developer's SSH agent or credential
# helper, and on CI whatever access the workflow grants.
[net]
git-fetch-with-cli = true
